apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.frontendService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.frontendService.name }}
    {{- include "observeflow.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.frontendService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.frontendService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.frontendService.name }}
    spec:
      serviceAccountName: observeflow-sa
      containers:
        - name: {{ .Values.frontendService.name }}
          image: "{{ .Values.frontendService.image.repository }}:{{ .Values.frontendService.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.frontendService.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.frontendService.port }}
          resources:
            {{- toYaml .Values.frontendService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.frontendService.port }}
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.frontendService.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
