{{/*
  Instrumentation CR is NOT deployed with helm install.
  It must be applied AFTER the OTel Operator is running:
  
  kubectl apply -f D:\ObservaFlow\helm\observeflow\instrumentation-cr.yaml
  
  This is because the OTel Operator webhook needs to be ready
  before it can validate/mutate Instrumentation resources.
*/}}
