apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.inventoryService.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.inventoryService.name }}
    {{- include "observeflow.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.inventoryService.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.inventoryService.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.inventoryService.name }}
      {{- if .Values.observability.enabled }}
      annotations:
        instrumentation.opentelemetry.io/inject-java: "true"
      {{- end }}
    spec:
      serviceAccountName: observeflow-sa
      containers:
        - name: {{ .Values.inventoryService.name }}
          image: "{{ .Values.inventoryService.image.repository }}:{{ .Values.inventoryService.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.inventoryService.image.pullPolicy | default "IfNotPresent" }}
          ports:
            - containerPort: {{ .Values.inventoryService.port }}
          env:
            - name: OTEL_SERVICE_NAME
              value: {{ .Values.inventoryService.name }}
          resources:
            {{- toYaml .Values.inventoryService.resources | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.inventoryService.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.inventoryService.port }}
            initialDelaySeconds: 20
            periodSeconds: 5
